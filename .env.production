# saas-identity-platform-react 生产环境变量(Vite build-time 烘焙)

# 后端地址 —— saas-react prod 直连 saas-springboot（v0.3.18 起）。
# 历史：早期版本经 saas-nextjs BFF (`https://saas-nextjs.xiangru.uk/api/v1/*`)
#         转发到 springboot, 但那层 BFF 仅是无状态 proxy;
#         现在 dev/prod 一致都直连 springboot, 减少一跳 + 排查链短。
# 路径前缀: Spring Security 把全站挂在 /api/v1 下, route 是 `/api/v1/{resource}`。
# VITE_API_MODE 仅 UI 标签 (不算真实模式), 显示成 'springboot' 与实际后端对齐。
VITE_API_BASE_URL=https://saas-springboot.xiangru.uk/api/v1
VITE_ENABLE_MSW=false
VITE_API_MODE=springboot